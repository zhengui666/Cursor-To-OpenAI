# 服务器端口 (默认: 3010)
PORT=3010

# Cursor 客户端版本 (默认: 1.1.3)
CURSOR_CLIENT_VERSION=1.1.3

# Cursor 时区配置 (默认: Asia/Shanghai)
CURSOR_TIMEZONE=Asia/Shanghai

# 日志格式 (默认: tiny)
# 可选值: combined, common, dev, short, tiny
MORGAN_FORMAT=tiny

# Cursor Cookie (可选，用于手动认证)
# 如果自动认证失败，可以通过 npm run login 获取后设置此变量
# CURSOR_COOKIE=your_cookie_here
