# Server port (default: 3010)
PORT=3010

# Cursor client version (default: 1.1.3)
CURSOR_CLIENT_VERSION=1.1.3

# Cursor timezone configuration (default: Asia/Shanghai)
CURSOR_TIMEZONE=Asia/Shanghai

# Log format (default: tiny)
# Options: combined, common, dev, short, tiny
MORGAN_FORMAT=tiny

# Cursor Cookie (optional, for manual authentication)
# If auto-authentication fails, you can get this value via npm run login
# CURSOR_COOKIE=your_cookie_here
